{
  "name": "weight-tracker-ios-mac",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prewww": "rm -rf www && mkdir www",
    "www": "cp -R ../dist/weight-tracker/* ./www/ || cp -R ../dist/* ./www/ || true",
    "sync": "npx cap sync ios",
    "open": "npx cap open ios",
    "build": "npm run prewww && npm run www && npm run sync",
    "clean": "rm -rf node_modules www ios",
    "postinstall": "npx cap doctor || true"
  },
  "dependencies": {
    "@capacitor-community/admob": "^7.2.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^6.2.1",
    "@capacitor/ios": "^6.2.1",
    "@capacitor/preferences": "^6.0.3",
    "@tensorflow/tfjs": "^4.22.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "description": "- macOS with Xcode 15+ - Node 18+ - Apple Developer account (to sign/runs on devices & Mac Catalyst) - Google AdMob account (use test IDs while developing)",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
